---
services:
  box:
    build:
      context: ./dockerart
      dockerfile: Dockerfile
    #conteiner_name: art
    volumes:
      - ./vol:/boxfuse-sample-java-war-hello/target
  war:
    build:
      context: ./dockerwar
      dockerfile: Dockerfile
    volumes:
      - ./vol:/usr/local/tomcat/webapps
    ports:
      - "8080:8080"
